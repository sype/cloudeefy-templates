apiVersion: v1
kind: ConfigMap
metadata:
  name: {{APP_NAME}}-config
  namespace: {{NAMESPACE}}
  labels:
    app: {{APP_NAME}}
    framework: react
data:
  # Application configuration
  # Note: React env vars are baked into the build at build-time
  # This ConfigMap can store runtime configuration for nginx or other purposes
  nginx.conf: |
    # This will be mounted if custom nginx config is needed
    # By default, the nginx.conf is baked into the Docker image
